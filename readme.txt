these funcitons are done

History :
a permanent file is stored in home directory,and every command is firstly added in it based on conditions and then the commands are shown by retriecving from that if input is history command

echo :
used string strtok for "\"" delimiter and then manipulated the string to form answer required and printed

current_dir:
is done by refering to the getcwd command and printing input

ls:
ls is done using struct stat and struct passwd and struct group as well as manipulating string on the basis of input given

cd :
used chdir with absolute path related to home directory

pwd :
used getcwd command

execvp:
used to run build in funcitons,created a child and parent waited for it untill the process is finished



